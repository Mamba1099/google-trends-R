# install required packages

# Install required packages
packages_to_install <- c(
  "tidyverse",
  "knitr",
  "tinytex",
  "moderndive",
  "shiny",
  "styler",
  "utils",
  "fivethirtyeight",
  "httr",
  "httpuv",
  "gtrendsR"
)

# Install packages
install.packages(packages_to_install)
